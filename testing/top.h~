#include "systemc.h"

SC_MODULE(top) {
	
	sc_in_clk clock;
	sc_out<bool> newimage,sending,start;
	sc_out<sc_uint<24> > RGB_24_bit;
	sc_in<bool> ready;
	sc_in<sc_int<16> >max_cols;
	sc_in<sc_int<16> >max_rows;

	void send_pixel();
	
	SC_CTOR(top) {
		SC_CTHREAD(send_pixel, clock.pos());
	}
};
